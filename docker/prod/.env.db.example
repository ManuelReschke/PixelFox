# PixelFox Database (MySQL 8.4) – .env (example)

# Root password for MySQL
DB_ROOT_PASSWORD=change_root_pw

# Application database and user
DB_NAME=pixelfox_db
DB_USER=pixelfox
DB_PASSWORD=change_user_pw

